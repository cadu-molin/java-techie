server:
  port: 9192

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  application:
    name: ORDER-SERVICE
  cloud:
    config:
      uri:
        -http://localhost:9196